<% layout('layout') -%>
<div class="span12" id="categories">
  <h1>All Categories</h1>
  <% if (alert) { %>
  <div class="alert alert-warning">
    <p><b>Error</b>: could not find the category <b><%= alert %></b>. You will find the complete list of available categories below.</p>
  </div>
  <% }
    if (error) { %>
  <div class="alert alert-danger">
    <p><%= error %></p>
  </div>
  <% } else { %>
    <%- utils.displayCategoryTree(catTree, 2) %>
  <% } %>
